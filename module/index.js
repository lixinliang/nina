'use strict';

const createTray = require('./tray.js');
const createMenu = require('./menu.js');
const createIPC = require('./ipc.js');

module.exports = {
    createTray,
    createMenu,
    createIPC,
};
